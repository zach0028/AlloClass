FROM python:3.12-slim

WORKDIR /app

COPY --from=ghcr.io/astral-sh/uv:latest /uv /usr/local/bin/uv

EXPOSE 8000

CMD ["sh", "-c", "uv sync --frozen && uv run alembic upgrade head && uvicorn app.main:app --host 0.0.0.0 --port 8000"]
